import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: "center",
    justifyContent: "center",
    padding: 20,
    backgroundColor: "#D5F7FF",
  },
  title: {
    fontSize: 24,
    fontWeight: "bold",
    marginBottom: 20,
    textAlign: "center",
    textTransform: "uppercase",
  },
  text: {
    fontSize: 18,
    marginBottom: 20,
    textAlign: "center",
  },
  list: { fontSize: 10, textAlign: "left" },
  button: {
    backgroundColor: "#2196f3",
  },
  btnText: {
    padding: 8,
    color: "white",
    textAlign: "center",
    textTransform: "uppercase",
  },
});
