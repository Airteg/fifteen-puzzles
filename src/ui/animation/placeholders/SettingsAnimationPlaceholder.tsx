import React, { Component } from "react";
import { Text, View } from "react-native";

export default class SettingsAnimationPlaceholder extends Component {
  render() {
    return (
      <View>
        <Text>SettingsAnimationPlaceholder</Text>
      </View>
    );
  }
}
